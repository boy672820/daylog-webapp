type Daily @model @auth(rules: [{allow: owner, ownerField: "owner"}])
{
  identityId: ID! @primaryKey(sortKeyFields: ["date"])
  date: AWSDate!
  content: String
  createdDate: AWSDateTime
  updatedDate: AWSDateTime
}