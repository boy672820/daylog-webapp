type Daily @model @auth(rules: [{allow: public, provider: iam, operations: [read, create, update]}])
{
  identityId: ID! @primaryKey(sortKeyFields: ["date"])
  date: AWSDate!
  content: String
  createdDate: AWSDateTime
  updatedDate: AWSDateTime
}